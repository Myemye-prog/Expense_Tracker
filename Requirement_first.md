🎯 First task

Don't build the whole Expense Tracker yet.

Start with just the data structure.

Create a Python file called:

expense_tracker.py

Your first task is to create 3 expenses using a list containing dictionaries.

For example, each expense should contain:

amount
category
description

Then write code that prints the expenses.